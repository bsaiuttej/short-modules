{
  "name": "local-store",
  "version": "1.0.0",
  "description": "Local storage for repeated asynchronous operation values for node application.",
  "main": "dist/index.js",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "start": "node dist/index.js",
    "predev": "npm run build",
    "dev": "node dist/index.js"
  },
  "keywords": [
    "local-store",
    "async-store"
  ],
  "author": "B SAI UTTEJ",
  "license": "ISC",
  "dependencies": {
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@types/node": "^20.3.1",
    "typescript": "^5.1.3"
  }
}
